# Verification

- `python test_static.py`: 12/12 PASS.
- `node --check app.js`: PASS.
- CSS brace balance: PASS (0 unmatched blocks).
- Local HTTP smoke test: `index.html`, `styles.css`, `app.js` all return HTTP 200.
- Demo remains `noindex,nofollow`.
- Knowledge Tree: exactly 8 branch cards, route modes = 5 subhub / 2 filter / 1 collection.
- Library preview: exactly 6 demo cards with client-side search/topic/brand/type filter.
- No `data:image` payload and no external script dependency.
- Responsive breakpoints include desktop/tablet/mobile and `prefers-reduced-motion`.
